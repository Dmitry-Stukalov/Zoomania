using System.Collections;
using System.Collections.Generic;
using System.Drawing;
using UnityEngine;
using UnityEngine.EventSystems;

public class Drag_Resource : MonoBehaviour
{/*
	private Vector3 Point { get; set; }
	private float Speed { get; set; }
	private bool IsMove { get; set; }


	public void Start()
	{
		IsMove = false;
	}

	public void Update()
	{
        if (IsMove)
        {
			gameObject.transform.position = Vector2.MoveTowards(transform.position, Point, Speed * Time.deltaTime);

			if (gameObject.transform.position.x == Point.x && gameObject.transform.position.y == Point.y) 
			{
				
				GetComponentInParent<Available_Resources>().PutResource(GetComponent<Resource_New>().GetCapacity());
				Destroy(gameObject);
			}
		}
	}*/
}
